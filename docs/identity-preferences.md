# Identity Preferences

Sexual orientation is optional sensitive profile data stored locally with the user profile. It is never inferred from uploaded images, never used to change avatar appearance or behavioral animation, and is included only in user-controlled data export. `Prefer not to say` is the default.

Avatar presentation is a separate visual preference for future generation providers. The current local stand-in remains neutral until the user creates a photo-derived avatar.
