import { useEffect, useState } from 'react'; import { App } from '../App'; import { SignupPage } from './SignupPage'; import { WelcomePage } from './WelcomePage';
export function SiteRouter(){const[path,setPath]=useState(window.location.pathname);useEffect(()=>{const handler=()=>setPath(window.location.pathname);window.addEventListener('popstate',handler);return()=>window.removeEventListener('popstate',handler)},[]);const navigate=(next:string)=>{history.pushState({},'',next);setPath(next);window.scrollTo({top:0,behavior:'smooth'})};if(window.eidosDesktop)return <App/>;if(path==='/signup')return <SignupPage navigate={navigate}/>;if(path==='/app')return <App/>;return <WelcomePage navigate={navigate}/>}
