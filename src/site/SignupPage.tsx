import { ArrowLeft } from 'lucide-react'; import { EidosLogo } from '../brand/EidosLogo'; import { ClosedBetaSignup } from '../pages/ClosedBetaSignup'; import { ThemeToggle } from '../theme/ThemeToggle';
export function SignupPage({ navigate }: { navigate: (path: string) => void }) { return <main className="public-site signup-page"><header className="public-nav"><button className="brand-button" onClick={() => navigate('/')}><EidosLogo/></button><nav><ThemeToggle/><button className="nav-text-button" onClick={() => navigate('/')}><ArrowLeft size={15}/>Back to welcome</button><button className="public-cta small" onClick={() => navigate('/app')}>Open Eidos</button></nav></header><section className="signup-public-shell"><div className="signup-intro"><p className="hero-eyebrow">Limited closed beta</p><h1>Build your Presence.<br/>Keep your voice.</h1><p>Join the first group testing camera-free communication with realistic visual presence and local-first privacy.</p><div className="signup-assurances"><span><b>01</b>No voice cloning</span><span><b>02</b>Local-first data</span><span><b>03</b>Camera off after training</span></div></div><ClosedBetaSignup publicMode/></section></main>; }

