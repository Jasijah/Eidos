import{InviteService,type InviteCode}from'../invites/InviteService';export interface ReferralSummary{invitesSent:number;invitesAccepted:number;activeReferrals:number;}
export class ReferralService{constructor(private invites=new InviteService()){}provision(userId:string):InviteCode[]{const existing=this.invites.list().filter(i=>i.creator===userId);if(existing.length>=3)return existing.slice(0,3);const needed=3-existing.length;const created=Array.from({length:needed},()=>this.invites.create({creator:userId,cohort:'beta',expiresAt:new Date(Date.now()+30*86400000).toISOString()}));return[...existing,...created]}summary(userId:string):ReferralSummary{const values=this.invites.list().filter(i=>i.creator===userId);const accepted=values.reduce((sum,i)=>sum+i.usageCount,0);return{invitesSent:values.length,invitesAccepted:accepted,activeReferrals:values.filter(i=>i.usageCount>0).length}}}