import { BookOpen, ExternalLink, LifeBuoy, MonitorUp, PlayCircle } from 'lucide-react';
const faqs = [['Does Eidos change my voice?', 'No. Your real microphone audio is always used. Eidos never clones or generates speech.'], ['Can I turn my camera off?', 'Yes. After optional training, Voice-Only Mode uses the universal and personalized behavior models without a camera.'], ['How do I use Eidos in a meeting?', 'Open the dedicated output, capture it in OBS, start OBS Virtual Camera, then select it in your meeting app.'], ['Where are my photos stored?', 'Locally by default. Third-party processing is disabled unless you explicitly opt in.']];
export function HelpCenter({ onSystemCheck, onVirtualCamera }: { onSystemCheck: () => void; onVirtualCamera: () => void }) { return <div className="help-layout"><section><p className="field-label">Help Center</p><h3>Get from setup to your first meeting.</h3><p>Start with the quick path, then use diagnostics if anything feels uncertain.</p><div className="quick-start"><button onClick={onSystemCheck}><LifeBuoy /><span><strong>1. Run System Check</strong><small>Confirm microphone, renderer, storage, and output readiness.</small></span></button><button onClick={onVirtualCamera}><MonitorUp /><span><strong>2. Open Virtual Camera Setup</strong><small>Launch the Eidos output and connect OBS.</small></span></button><a href="mailto:support@eidospresence.com?subject=Eidos%20Beta%20Issue"><ExternalLink /><span><strong>3. Contact beta support</strong><small>Include your exported diagnostics with a bug report.</small></span></a></div></section><aside className="panel p-5"><h3 className="section-title">Frequently asked</h3><div className="faq-list mt-4">{faqs.map(([question, answer]) => <details key={question}><summary>{question}</summary><p>{answer}</p></details>)}</div><div className="video-placeholder mt-5"><PlayCircle /><span><strong>Video walkthrough</strong><small>Closed beta recording placeholder</small></span></div><a className="secondary-button mt-4 w-full" href="/docs/beta-user-guide.md"><BookOpen size={16} />Beta User Guide</a></aside></div>; }
