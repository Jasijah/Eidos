import React from 'react';import{renderToString}from'react-dom/server';import{describe,expect,it}from'vitest';import{BetaDashboard}from'./BetaDashboard';describe('BetaDashboard',()=>{it('renders adoption, quality, privacy, and analytics controls',()=>{localStorage.clear();const html=renderToString(React.createElement(BetaDashboard));expect(html).toContain('Camera-off use');expect(html).toContain('Presence quality');expect(html).toContain('Anonymous analytics');expect(html).toContain('Privacy: local-only')})})