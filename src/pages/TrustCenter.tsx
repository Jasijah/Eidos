import { Download, EyeOff, KeyRound, Mic, ShieldCheck, Trash2 } from 'lucide-react';
const items = [
  { icon: <Mic />, title: 'Your voice stays yours', copy: 'Eidos uses your live microphone. It never clones your voice, generates speech, or speaks for you.' },
  { icon: <EyeOff />, title: 'Local-first processing', copy: 'Camera tracking, behavior memory, and avatar identity stay on your device by default.' },
  { icon: <KeyRound />, title: 'Encrypted records', copy: 'Sensitive local records are encrypted. Desktop Eidos uses the operating system key store when available.' },
  { icon: <ShieldCheck />, title: 'Consent before processing', copy: 'Photos require explicit consent. No third-party avatar provider receives images unless you separately opt in.' }
];
export function TrustCenter({ onPrivacy }: { onPrivacy: () => void }) { return <div className="space-y-5"><section className="trust-hero"><p className="field-label">Trust Center</p><h3>Your Presence. Your voice. Your data.</h3><p>Eidos replaces the visual camera layer only. You remain the person speaking and stay in control of what is stored.</p></section><section className="trust-grid">{items.map((item) => <article className="panel p-5" key={item.title}><span>{item.icon}</span><h4>{item.title}</h4><p>{item.copy}</p></article>)}</section><section className="panel p-5"><h3 className="section-title">Simple data controls</h3><div className="mt-4 grid gap-3 md:grid-cols-3"><div className="trust-action"><Download /><strong>Export</strong><p>Download your avatar, behavior profile, and consent settings.</p></div><div className="trust-action"><Trash2 /><strong>Delete</strong><p>Remove images, identity, behavior memory, or everything.</p></div><div className="trust-action"><ShieldCheck /><strong>Review consent</strong><p>Keep cloud and public model training disabled by default.</p></div></div><button className="primary-button mt-5" onClick={onPrivacy}>Open my privacy controls</button></section></div>; }
