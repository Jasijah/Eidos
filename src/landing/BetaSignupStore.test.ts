import{describe,expect,it}from'vitest';import{BetaSignupStore}from'./BetaSignupStore';describe('BetaSignupStore',()=>{it('validates and exports beta signups',()=>{localStorage.clear();const store=new BetaSignupStore();expect(()=>store.save({name:'A',email:'bad',company:'',jobTitle:'',weeklyMeetings:2,interest:'privacy'})).toThrow();store.save({name:'A',email:'a@example.com',company:'Eidos',jobTitle:'Lead',weeklyMeetings:10,interest:'camera off'});expect(store.exportCsv()).toContain('a@example.com')})})