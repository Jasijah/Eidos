import{describe,expect,it}from'vitest';import{validateHandshake}from'./FrameTransportProtocol';describe('native frame protocol',()=>{it('requires disclosure and bounded output',()=>{expect(validateHandshake({version:1,width:1280,height:720,frameRate:30,pixelFormat:'BGRA',disclosureActive:true})).toBe(true);expect(validateHandshake({version:1,width:1280,height:720,frameRate:30,pixelFormat:'BGRA',disclosureActive:false})).toBe(false);});});