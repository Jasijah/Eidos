import { Download, RefreshCw } from 'lucide-react';
export function RecoveryNotice({ message, onRetry, onDiagnostics }: { message: string; onRetry: () => void; onDiagnostics: () => void }) { return <section className="recovery-notice" role="alert"><div><strong>That did not work yet.</strong><p>{message} Your data is safe, and Voice-Only Mode remains available.</p></div><div><button className="secondary-button" onClick={onRetry}><RefreshCw size={15} />Retry</button><button className="secondary-button" onClick={onDiagnostics}><Download size={15} />Run diagnostics</button><a href="mailto:support@eidospresence.com?subject=Eidos%20Beta%20Help">Support</a></div></section>; }
