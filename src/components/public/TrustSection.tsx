import { LockKeyhole, Mic2, ShieldCheck, Trash2 } from 'lucide-react';
const principles = [[Mic2,'No voice cloning','Eidos never generates speech and never speaks for you.'],[LockKeyhole,'Local-first privacy','Sensitive identity and behavior data stays on your device by default.'],[ShieldCheck,'Consent by design','Images are not used for training unless you separately opt in.'],[Trash2,'User data controls','Export or delete your avatar and behavior data at any time.']] as const;
export function TrustSection() { return <section className="trust-section" id="trust"><div className="public-section-heading"><p className="public-section-label">Trust is the product</p><h2>Your presence. Your voice. Your control.</h2></div><div className="trust-list">{principles.map(([Icon,title,copy]) => <article key={title}><Icon /><h3>{title}</h3><p>{copy}</p></article>)}</div></section>; }
